var currAge = 25;
var maxAge = 96;
var amount = 2;

for (var i = 0; i <= maxAge; i++) {
	var total = (maxAge - currAge) * (365 * amount)
}	console.log("You will need " + total + " to last you until the ripe old age of " + maxAge + ".");

>> You will need 51830 to last you until the ripe old age of 96.
